spring:
  data:
    mongodb:
      host: 192.168.111.128
      port: 27017
      username: root
      password: root
      # 指定操作的数据库
      database: file_server

  # consul配置
  cloud:
    consul:
      enabled: false
      discovery:
        enabled: false
